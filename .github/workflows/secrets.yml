name: Secrets
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    env:
      PASSWORD: ${{ secrets.PASSWORD }} # Secretsの参照
    steps:
      # 実運用では、以下のようにログ出力は絶対に避ける
      - run: echo "${PASSWORD}" # ログ出力はマスクされる
      - run: echo "${PASSWORD:0:1} ${PASSWORD#?}" # ログ出力はマスクされない