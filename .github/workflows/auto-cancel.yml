name: Auto cancel
on: pull_request
concurrency:
  # Concurrencyグループにブランチ名（github.ref）を含めることで、異なるPRではキャンセルされないようにする
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  sleep:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 120